// Forge commercial variant configuration.
//
// Keep product-specific defaults here so upstream openGym changes stay easy to merge.
export const BRAND_NAME = 'Forge'
export const DEFAULT_LANG = 'pt-BR'

// Temporarily enabled so the Forge product can be tested with the same exercise imagery and
// animations used by upstream openGym. The media remains third-party content and is not
// redistributed by this repository; production/commercial usage still needs its own rights review.
export const EXERCISE_MEDIA_ENABLED = true

// AGPL source for the running modified version.
export const SOURCE_REPO = 'https://github.com/le0andrade/openGym'
